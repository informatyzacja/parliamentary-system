import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  fonts: {
    body: "Lato, -apple-system, Arial, sans-serif",
    heading: "Lato, -apple-system, Arial, sans-serif",
  },
});
