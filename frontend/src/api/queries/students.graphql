query Students($termId: ID!) {
  students(filters: { term_of_offices: { id: { eq: $termId } } }) {
    data {
      id
      attributes {
        name
        surname
        student_number
        functions {
          data {
            id
            attributes {
              name
            }
          }
        }
      }
    }
  }
}
