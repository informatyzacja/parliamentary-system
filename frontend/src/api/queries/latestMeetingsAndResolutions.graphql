query LatestMeetingsAndResolutions {
  meetings(sort: ["date:desc", "id:desc"], pagination: { limit: 10 }) {
    data {
      id
      attributes {
        date
        name
      }
    }
  }
  resolutions(
    sort: ["id:desc", "publishedAt:desc"]
    pagination: { limit: 10 }
  ) {
    data {
      id
      attributes {
        name
        publishedAt
        document {
          data {
            attributes {
              url
            }
          }
        }
      }
    }
  }
}
